<%= form_for(@pstat) do |f| %>
  <% if @pstat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pstat.errors.count, "error") %> prohibited this pstat from being saved:</h2>

      <ul>
      <% @pstat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
