<%= form_for(@ctype) do |f| %>
  <% if @ctype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ctype.errors.count, "error") %> prohibited this ctype from being saved:</h2>

      <ul>
      <% @ctype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, class: "label1" %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit "Submit", class: "btn btn-success btn-sm" %>
  </div>
<% end %>
